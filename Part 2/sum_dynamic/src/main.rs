use std::io;

fn main() {
    let mut vec: Vec<i32> = Vec::new();
    let mut size = String::new();

    // TOOD

    let s = sum(vec);
    println!("Sum = {s}");
}

fn sum(vec: Vec<i32>) -> i32 {
    // TODO
}
